[activate]
tree_markers = ["package.json"]

[capture]
env_allowlist = ["NODE_OPTIONS", "NODE_ENV", "UV_THREADPOOL_SIZE"]

[toolchain]
node = ["node", "--version"]

[lockfiles]
files = ["package-lock.json", "yarn.lock", "pnpm-lock.yaml", "bun.lockb", ".nvmrc"]
